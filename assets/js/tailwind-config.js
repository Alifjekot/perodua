tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        perodua: '#009639', /* Changed to Light Blue (sky-400) */
                        peroduaDark: '#007a2d', /* Changed to Dark Blue (sky-600) */
                        brandSlate: '#0f172a'
                    }
                }
            }
        };